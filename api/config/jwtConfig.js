module.exports = {
/* É o secret requerido para o JWT encriptar e desencriptar as tokens.
   Tipicamente, seria guardado como uma variável de ambiente num ficheiro
   que não replique para o GitHub.
*/
    secret: 'jwt-secret',
};